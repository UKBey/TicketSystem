apiVersion: v1
kind: Service
metadata:
  name: llm-service
  labels:
    app.kubernetes.io/name: llm-service
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: llm-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
