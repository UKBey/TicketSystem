apiVersion: v1
kind: Service
metadata:
  name: keycloak-iam
  labels:
    app.kubernetes.io/name: keycloak-iam
    app.kubernetes.io/component: identity
spec:
  selector:
    app.kubernetes.io/name: keycloak-iam
  ports:
    - name: http
      port: 8080
      targetPort: 8080
