spring:
  datasource:
    url: jdbc:tc:postgresql://localhost:5432/ticketdb
    username: ticketdb
    password: 321654
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration