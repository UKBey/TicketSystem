spring:
  datasource:
    url: jdbc:tc:postgresql://localhost:5432/ticketdb
    username: ticketdb
    password: 321654
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE
  flyway:
    enabled: true
    locations: classpath:db/migration